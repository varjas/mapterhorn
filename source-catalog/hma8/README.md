## Authentication

Downloads require a free NASA Earthdata Login account.

1. Register at: https://urs.earthdata.nasa.gov/users/new
2. Generate a token at: https://urs.earthdata.nasa.gov/profile

Set the token as an environment variable before running the pipeline:
```bash
export EARTHDATA_TOKEN="your_token_here"
```
