# au5 Dataset

The file list is split into EPSG-specific directories (e.g. `au5b`, `au5c`, `au5d`) to streamline processing due to varying CRS projections across source files.

The Justfile is shared across all related directories via symlinks to the `au5a` directory.