# au5 Dataset

The file list is split into EPSG-specific nested directories to streamline processing due to varying CRS projections across source files.

The LICENSE.pdf and metadata.json files are shared across all nested directories via symlinks.